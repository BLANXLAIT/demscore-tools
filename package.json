{
  "name": "demscore-tools",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @demscore/ucdp -w @demscore/views && npm run build -w @demscore/mcp-server",
    "test": "npm test --workspaces",
    "lint": "npm run lint --workspaces"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@types/node": "^25.3.3",
    "tsx": "^4.21.0"
  }
}
